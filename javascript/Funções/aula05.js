// Função que retorna o comprimento de uma string
function comprimentoString(texto) {
    return texto.length;
}

const palavra = "Hello";
const tamanho = comprimentoString(palavra);
console.log(tamanho)
